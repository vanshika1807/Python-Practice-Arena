# variable = a container to store the value
# python is dynamically typed so no need to dfine the data type
# Naming convention = conevention for naming things (agreed scheme for naming)
# there are many reserve words which cannot be used as variable name >.<
# we need to call var anywehere is order to make them work

seconds = 24 * 60 * 60
name_of_unit = "seconds"

print(f"seconds in 100 day {100 * seconds} {name_of_unit}")
print(f"seconds in 50 day {50 * seconds} {name_of_unit}")
print(f"seconds in 20 day {20 * seconds} {name_of_unit}")


